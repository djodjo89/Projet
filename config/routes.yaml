perso:
    path: /character/{id}
    controller: App\Controller\PersoControleur::character
    requirements:
        id: \d+

accueil:
    path: /hello/{nom}
    controller: App\Controller\Accueil::bienvenue
    requirements:
        nom: "[a-zA-Z]+"